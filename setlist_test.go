package main

import "testing"

func TestSetlist(t *testing.T) {
	testCases := []struct {
		name string
	}{
		// {
		// 	name: "no name provided returns the temp setlist",
		// },
		// {
		// 	name: "no name provided creates and returns temp setlist if doesn't exist",
		// },
		// {
		// 	name: "name provided returns persisted setlist when matched",
		// },
		// {
		// 	name: "name provided returns not found message when unmatched",
		// },
	}

	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}
